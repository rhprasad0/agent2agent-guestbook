# AWS Configuration
AWS_REGION=us-east-1
DYNAMODB_TABLE_NAME=a2a-guestbook-messages

# API Keys (JSON array, injected from K8s Secret via External Secrets Operator)
API_KEYS=["dev-key-change-this","test-key-change-this"]

# Application Configuration
RATE_LIMIT_PER_MINUTE=10
LOG_LEVEL=INFO
PORT=8000
